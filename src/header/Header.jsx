import React from "react";
import { Flex, Text } from "theme-ui";

const Header = () => {
  return (
    <Flex sx={{ backgroundColor: "#fff" }}>
      <Text sx={{ color: "primary" }}>Header</Text>
    </Flex>
  );
};

export default Header;
